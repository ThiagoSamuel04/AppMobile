import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    backgroundColor: 'red',
    padding: 20,
    alignItems: 'center'
  },
  title: {
    fontSize: 20,
    color: 'white'
  }
});